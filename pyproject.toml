[build-system]
requires = ["setuptools >= 61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "multiclean"
authors = [{name = "Nick Wright", email = "nicholas.wright@dpird.wa.gov.au"}]
dynamic = ["version"]
description = "Python library for morphological cleaning of multiclass 2D numpy arrays (edge smoothing and island removal)"
license = "MIT"
readme = {file = "README.md", content-type = "text/markdown"}
keywords = [
    "morphological-operations",
    "multiclass", 
    "classification-arrays",
    "image-processing",
    "edge-cleaning",
    "island-removal",
    "segmentation",
    "post-processing",
    "numpy",
    "opencv",
    "remote-sensing",
    "computer-vision",
    "noise-reduction"
]
requires-python = ">=3.9"
dependencies = [
    "opencv-python>=4.0",
]
[dependency-groups]
dev = [
    "jupyter>=1.0",
    "matplotlib>=3.0",
    "mypy>=1.19",
    "pre-commit>=4.0",
    "rasterio>=1.0",
    "ruff>=0.15.8",
    "pytest>=8.0",
]

[tool.mypy]
ignore_missing_imports = true
[tool.setuptools.dynamic]
version = {attr = "multiclean.__version__.__version__"}


[project.urls]
Homepage = "https://github.com/DPIRD-DMA/MultiClean"
Repository = "https://github.com/DPIRD-DMA/MultiClean"
Changelog = "https://github.com/DPIRD-DMA/MultiClean/blob/main/CHANGELOG.md"


[tool.setuptools]
packages = ["multiclean"]

[tool.setuptools.package-data]
multiclean = ["py.typed"]

[tool.ruff.lint]
select = ["E", "F", "B"]
